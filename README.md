# 前端fe工具

```
  Usage: template|t [options]

  下载模板

  Options:

    -h, --help                  output usage information
    -v, --version [moduleName]  模板版本
    -n, --name [fileName]       模板输出名称

 Examples:

$ fe t
or
$ fe t -v 1 -n template
```